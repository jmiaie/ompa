"""
OMPA test suite — split into per-module files.

The full test suite lives in:
  test_core.py               — Ompa integration
  test_vault.py              — Vault management & orphan detection
  test_hooks.py              — Lifecycle hooks
  test_mcp_server.py         — MCP server tool dispatch
  test_dual_vault.py         — Dual-vault architecture
  test_semantic.py           — SemanticIndex
  test_agent_integration.py  — Session cold-start safety
  test_classifier.py         — MessageClassifier (already separate)
  test_knowledge_graph.py    — KnowledgeGraph (already separate)
  test_palace.py             — Palace (already separate)
  test_properties.py         — Property-based tests (already separate)
"""
